# secrets — never commit
.env
*.key
.akey
.aid

# local audio + generated outputs (may contain PHI)
audio/
chunks/
results/
data/
onset_analysis/
*.wav
*.webm

# local-only real-data scenarios (may contain PHI) + agent run outputs
mock_agent/local/
agent/*.jsonl

# python
__pycache__/
*.pyc
.pytest_cache/
*.egg-info/
.venv/
.DS_Store
