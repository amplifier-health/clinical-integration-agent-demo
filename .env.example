# Amplifier Health API credentials (get yours at https://api.amplifierhealth.com)
# These are read at runtime; never commit real values.
AMPLIFIER_API_KEY=your-api-key
AMPLIFIER_API_ID=your-account-id
